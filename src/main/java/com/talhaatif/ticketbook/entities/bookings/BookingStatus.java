package com.talhaatif.ticketbook.entities.bookings;


//An enum is a special "class" that represents a group of constants, final variables, immutable
//Examples: month days, days, colors, deck of cards
public enum BookingStatus {
    PENDING,
    CONFIRMED,
    CANCELLED
}

